# Feature Spec -> Saving data in local data/ directory

## Requirements
---
- Need to remove the hardcoded obs/int and ui/ne path saving
- everything should live in data/ directory as data/subject/session
- need to no longer resave data that is already there (same with plots) -> this clogs commit history REALLY bad