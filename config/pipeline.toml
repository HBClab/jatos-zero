schema_version = 1

[pipeline]
enable_plots = false

[pipeline.outputs]
enable_saved_data = true
data_root_path = "."
data_folder_name = "data"

[pipeline.defaults.columns]
subject = "subject_id"
session = "session_number"

[pipeline.tasks.NNB]
domain = "cc"
task_ids = [1035, 1036, 1037]

[pipeline.tasks.NNB.qc]
threshold = 0.5
max_rt = 1800
